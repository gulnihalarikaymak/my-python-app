# my-python-app
CI-CD LESSON
## Deployment Overview

This Flask application is hosted on an Azure Virtual Machine.
The app is containerized with Docker and deployed using Coolify.
A CI/CD pipeline is configured with GitHub Actions.
Every push to the repository automatically triggers tests and redeployment.
